// Generated by CoffeeScript 1.6.2
(function() {
  $(function() {
    return window.thumbnail = function(moves, opts) {
      var size, tn, _ref;

      if (opts == null) {
        opts = {};
      }
      size = (_ref = opts.size) != null ? _ref : 200;
      _.defaults(opts, {
        NINE_POINTS_RADIUS: 1.5,
        LINE_COLOR: '#aaa',
        click: false
      });
      opts.PAWN_RADIUS = Math.round(size * .7 / 38);
      opts.margin = Math.round(opts.PAWN_RADIUS * 1.4);
      opts.size = size - 2 * opts.margin;
      tn = $("<div class='thumb'><canvas class='draw'/></div>");
      tn.data('game', {
        moves: moves
      });
      if (opts.title) {
        tn.append("<span class='title'>" + opts.title + "</span>");
      }
      new CanvasBoard(tn, opts);
      return tn;
    };
  });

}).call(this);
